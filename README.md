# Playwrite-CRX-pro
A tool that records browser interactions and automatically generates Playwright test scripts. Capture clicks, form inputs, and navigation in real time, then export them as ready-to-run automation code — no manual scripting required.
